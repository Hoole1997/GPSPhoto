ext {
    // AdMob配置
    admob = [applicationId: "ca-app-pub-7869168547621070~4025911469", // 测试应用ID，请替换为实际的AdMob应用ID
             adUnitIds    : [banner      : "",        // 横幅广告测试ID
                             interstitial: "ca-app-pub-7869168547621070/3642768081", // 插页广告测试ID
                             splash      : "ca-app-pub-7869168547621070/3011083391",       // 开屏广告测试ID
                             native      : "ca-app-pub-7869168547621070/1698001724",        // 原生广告测试ID
                             full_native : "ca-app-pub-7869168547621070/1698001724",        // 全屏原生广告测试ID
                             rewarded    : "ca-app-pub-7869168547621070/8703523073"         // 激励广告测试ID
             ]]

    // Google Ad Manager 配置（按业务替换为正式 ID）
    gam = [adUnitIds: [banner      : "",
                       interstitial: "/23312177113/Photo_IV",
                       splash      : "/23312177113/Photo_SP",
                       native      : "",
                       full_native : "",
                       rewarded    : ""
    ]]

    // Pangle配置
    pangle = [applicationId: "8845910", // Pangle测试应用ID
              adUnitIds    : [splash      : "890162715",        // 开屏广告测试ID（竖屏）
                              banner      : "",        // 横幅广告测试ID（320x50）
                              interstitial: "983290010",       // 插页广告测试ID（竖屏）
                              native      : "983290048",        // 原生广告测试ID
                              full_native : "983290048",        // 全屏原生广告测试ID
                              rewarded    : "983290091"         // 激励视频测试ID（竖屏）
              ]]

    // TopOn配置
    topon = [applicationId: "h1heegc5pfs60i",           // TopOn 应用 ID（需替换为实际值）
             appKey       : "a12214b95fc0b179253d78a32f6b1dc67",          // TopOn 应用密钥（需替换为实际值）
             adUnitIds    : [interstitial: "n1heegc5s0fj67",  // 插页广告位 ID（需替换为实际值）
                             rewarded    : "n1heegc5rimadm",  // 激励广告位 ID
                             native      : "n1heegc5srbgkm",  // 原生广告位 ID（需替换为实际值）
                             splash      : "n1heegc5t8ktn2",  // 开屏广告位 ID（需替换为实际值）
                             full_native : "n1heegc5srbgkm",  // 全屏原生广告位 ID（需替换为实际值）
                             banner      : ""]  // 横幅广告位 ID（需替换为实际值）
    ]

    // MAX (AppLovin) 配置
    max = [sdkKey   : "",  // Demo SDK Key（测试用）
           adUnitIds: [splash      : "",      // 开屏广告位 ID
                       banner      : "",      // 横幅广告位 ID
                       interstitial: "",// 插页广告位 ID
                       native      : "",      // 原生广告位 ID
                       fullNative  : "", // 全屏原生广告位 ID
                       rewarded    : ""]    // 激励广告位 ID
    ]

    // 应用版本配置
    app = [applicationId: "com.gpsphoto.locationmap.tool",
           compileSdk   : 36,
           minSdk       : 26,
           targetSdk    : 36,
           versionCode  : 2,
           versionName  : "1.0.1"]

    // 统计归因配置
    analytics = [adjustAppToken: "hopjs50gmuww",           // Adjust App Token
                 thinkingDataAppId: "871c0ebde6be4329b9b74acb3f08ba36",   // 数数 SDK APP ID
                 thinkingDataServerUrl: "https://ds.devs343.com",     // 数数上报域名
                 defaultUserChannel: "natural"]                           // 默认用户渠道，internal默认paid
}
